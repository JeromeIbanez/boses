# Dependencies for the MCP server only.
# This runs in a separate Docker container (Dockerfile.mcp) so it doesn't
# share a venv with the main FastAPI backend.
mcp[cli]>=1.6.0
httpx>=0.27.0
python-dotenv>=1.0.0
fastapi>=0.111.0
uvicorn>=0.30.0
sentry-sdk>=2.0.0
